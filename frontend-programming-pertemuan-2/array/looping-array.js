// Membuat variable array menggunakan const
const animals = ["Kucing", "Anjing", "Burung"];

// Looping array menggunakan for
for (let i = 0; i < animals.length; i++) {
  console.log(`Hewan: ${animals[i]}`);
}
